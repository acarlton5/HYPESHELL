# Legacy Plugins

This folder is retained for compatibility while HypeShell migrates to first-class `HYPEMODULE`, `HYPEGADGET`, and `HYPETHEME` packages.

New installable functionality should come through HypeStore package records.
